# Environment Variables for cPanel
# Add these in the "Setup Node.js App" -> "Add Variable" section.

NODE_ENV=production
# PORT is set automatically by cPanel

# App URLs
APP_URL=https://crm.scriptishrx.com
FRONTEND_URL=https://crm.scriptishrx.com

# Security
JWT_SECRET= # Generate a strong random string (e.g., `openssl rand -base64 32`)
REFRESH_SECRET= # Generate another strong random string

# Database (Supabase)
SUPABASE_URL=
SUPABASE_ANON_KEY=
PUBLIC_SUPABASE_URL= # Same as SUPABASE_URL
PUBLIC_SUPABASE_ANON_KEY= # Same as SUPABASE_ANON_KEY

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4-turbo-preview

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_BASIC=
STRIPE_PRICE_INTERMEDIATE=
STRIPE_PRICE_ADVANCED=

# Twilio
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_API_KEY_SID=
TWILIO_API_KEY_SECRET=

# Email (SendGrid)
SENDGRID_API_KEY=
EMAIL_FROM= # e.g. support@scriptishrx.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
